# Kamal secrets template
# This file shows the structure of secrets needed for Kamal deployment
# DO NOT commit actual secrets to git!
# 
# For local deployment: Copy to .kamal/secrets and fill with actual values from Doppler
# For CI/CD deployment: Secrets are fetched directly from Doppler via GitHub Actions workflow
#
# To generate locally:
#   doppler secrets download --no-file --format env > .kamal/secrets

export TMDB_API_KEY=your_tmdb_api_key_here
export REDIS_URL=redis://redis:6380
export SENTRY_DSN=your_sentry_dsn_here
export SENTRY_ENVIRONMENT=production
export SESSION_SECRET=your_session_secret_here
export KAMAL_REGISTRY_PASSWORD=your_github_pat_here
