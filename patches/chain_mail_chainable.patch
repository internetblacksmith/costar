--- a/lib/chain_mail/chainable.rb
+++ b/lib/chain_mail/chainable.rb
@@ -9,12 +9,16 @@ module ChainMail
     def chainable(*methods)
       methods.each do |method|
         original = instance_method(method)
-        # Suppress the "method redefined" warning for methods already defined by Forwardable
-        Warning.suppress do
+        # Suppress "method redefined" warnings for methods already defined by Forwardable
+        old_verbose = $VERBOSE
+        $VERBOSE = nil
+        begin
           define_method method do |*args|
             original.bind(self).call(*args)
             self
           end
+        ensure
+          $VERBOSE = old_verbose
         end
       end
     end
